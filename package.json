{
  "name": "isolde",
  "private": true,
  "scripts": {
    "dev:web": "bun --filter @isolde/app dev",
    "dev:tauri:desktop": "bun run icon:dev && concurrently -n web,desktop -c cyan,blue -k \"bun run dev:web\" \"bun --filter @isolde/tauri dev\"",
    "dev:tauri:desktop:verbose": "bun run icon:dev && concurrently -n web,desktop -c cyan,blue -k \"bun run dev:web\" \"bun --filter @isolde/tauri dev -v\"",
    "dev:tauri:android": "bun run icon:dev && concurrently -n web,android -c cyan,green -k \"bun run dev:web --host\" \"bun --filter @isolde/tauri tauri android dev\"",
    "dev:tauri:android:verbose": "bun run icon:dev && concurrently -n web,android -c cyan,green -k \"bun run dev:web --host\" \"bun --filter @isolde/tauri tauri android dev -v\"",
    "// yes i know duplicates i do not care": "comment",
    "dev:all": "bun run icon:dev && concurrently -n web,desktop,android -c cyan,blue,green -k \"bun run dev:web --host\" \"bun --filter @isolde/tauri dev\" \"bun --filter @isolde/tauri tauri android dev\"",
    "dev:all:verbose": "bun run icon:dev && concurrently -n web,desktop,android -c cyan,blue,green -k \"bun run dev:web --host\" \"bun --filter @isolde/tauri dev -v\" \"bun --filter @isolde/tauri tauri android dev -v\"",
    "build:desktop": "bun run icon:release && bun --filter @isolde/tauri tauri build",
    "build:android:aab": "bun run icon:release && bun --filter @isolde/tauri tauri android build",
    "build:android:apk": "bun run icon:release && bun --filter @isolde/tauri tauri android build --apk",
    "build:all": "bun run icon:release && bun run build:desktop && bun run build:android:aab"
  },
  "workspaces": [
    "core/*",
    "shared/*"
  ],
  "devDependencies": {
    "concurrently": "^10.0.4",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "@types/node": "^26.1.2"
  }
}
