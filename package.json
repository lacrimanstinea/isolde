{
  "name": "isolde",
  "private": true,
  "scripts": {
    "dev:app": "bun --filter @isolde/app dev:web",
    "dev:tauri": "bun --filter @isolde/tauri dev",
    "build:app": "bun --filter @isolde/app build:web",
    "build:tauri": "bun --filter @isolde/tauri build"
  },
  "workspaces": [
    "core/*",
    "shared/*"
  ]
}
