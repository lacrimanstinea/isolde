export const STORAGE_KEYS = {
  PLATFORM: "isolde_platform",
  SETTINGS: "isolde_preferences",
};

export const PREFS_KEYS = {
  ALLOW_DESKTOP_SWIPE: STORAGE_KEYS.SETTINGS + "_allowDesktopSwipe",
  ALLOW_PULL_TO_REFRESH_DESKTOP:
    STORAGE_KEYS.SETTINGS + "_allowPullToRefreshDesktop",
};

// pretty much what i'll be using just so i keep it consistent
